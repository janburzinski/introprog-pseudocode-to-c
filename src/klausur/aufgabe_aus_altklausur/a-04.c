// gcc src/klausur/aufgabe_aus_altklausur/a-04.c -o builds/a-04.exe && ./builds/a-04.exe

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h> /* defines bool, true, false */
#include <stddef.h>  /* defines size_t */

/**
 *
 * Vervollständigen Sie folgende C-Funktion so, dass sie true genau dann zurückgibt, wenn im Eingabearray
 * der Länge len zwei Zahlen vorkommen, deren Summe genau 40 ist.
 * Sie dürfen nur Funktionen aufrufen, für die Sie auch eine Implementierung aufschreiben.
 *
 */

int two_sum_forty(int *arr, size_t len)
{
}

int main()
{
  return 0;
}